# All intermediate/generated artifacts: observation pools, entropy caches,
# quantization packs (tens of GB each), eval logs and summaries. None of
# this is source code -- it's all reproducible by re-running the scripts/
# pipelines against a checkpoint.
cache/

# Python
__pycache__/
*.pyc
.pytest_cache/

# OS
.DS_Store

# Local-only experiments kept out of the public repo (currently: sub-4-bit
# probing). The published repo covers the validated W4A4 recipe only.
scripts/probe_w3a3.sh
scripts/_local_w3a3_pack_builder.py
