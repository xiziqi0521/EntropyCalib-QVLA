# 技术机理详解（给自己看的版本）

> 这份文档不是给别人汇报用的（汇报用 `REPORT_CN.md`），是把系统里每一步
> "为什么这么做、出了什么问题、怎么修的"讲清楚，尽量少用行话、多用类比。

## 一、什么是 W4A4 的"校准"，为什么样本选择会影响精度

W4A4 = 权重（Weight）和激活值（Activation）都量化成 4-bit 整数。这不是训练，
是**训练后量化（PTQ）**：模型权重已经训练好了，我们要在不重新训练的前提下，
把浮点数权重和推理过程中的浮点激活值都压缩成 4-bit 整数，同时尽量不损失精度。

问题在于：4-bit 只有 16 个可表示的数值，而浮点权重/激活值的分布范围可能很大、
也可能有少数"离群值"（outlier）。直接暴力四舍五入会把大部分信息抹平。所以量化
需要先知道"这一层的激活值大概长什么样"（数值范围、分布形状），这一步就叫
**校准（calibration）**——喂给模型几个真实观测，跑一遍前向，记录下每一层实际
见到的激活值统计量。

我们用的两种量化方法，都依赖校准数据，但用法不同：

- **GPTQ**（用在 PaliGemma 语言模型的 126 层上）：不只是记录数值范围，而是用
  校准样本的激活值算出一个"敏感度矩阵"（数学上是 `H = XᵀX`，X 是校准激活值）。
  这个矩阵告诉你：这一层的权重，哪些方向的误差对最终输出影响大、哪些方向影响
  小。GPTQ 逐列量化权重，每量化一列就用这个敏感度信息把误差"补偿"到还没量化
  的其余权重上，让整体输出误差最小。**如果校准样本不能代表模型真实会见到的
  激活值分布，这个敏感度矩阵算出来的"哪里重要"就是错的**，GPTQ 会把补偿力气
  用错地方。
- **RTN**（用在 Expert 动作专家的 126 层上，per-step 版本）：更简单，直接按
  分组（group）取激活值的最大/最小值算出量化的缩放系数（scale），四舍五入。
  校准样本决定这个 scale 算得准不准——样本代表性不够，scale 要么留了太多余量
  （精度浪费），要么被离群值带偏（截断了本该保留的信息）。

**结论**：校准阶段"喂进去哪些观测"，直接决定了后面 GPTQ/RTN 数学计算出来的
量化参数好不好。这也是这整个项目的出发点——原来的校准样本选择是"盲选"，我们
想验证：换一种更聪明的选择方式，能不能在**校准样本数量不变**的前提下换来精度
提升。

## 二、两种"盲选"其实是同一种机制（重要澄清）

我原以为"我们的盲选"和"你原来 baseline 的盲选"是两种不同的采样方式，专门去
读代码查证后发现：**两者的采样机制几乎完全一样**，只是包了两层不同的脚本。

### 校准观测是怎么来的（两边共用同一个生成脚本）

`Omega-QVLA/tools/record_libero_obs_for_pi05.py` 这个脚本，**不会真的跑
pi0.5 策略**（不是让模型自己走一遍任务收集轨迹），也不是从人类示教数据集里
录制的（不是离线专家数据）。它做的是：

1. 把 LIBERO 环境重置到官方定义的"任务初始状态"
2. 让环境执行几步"什么都不做"的零动作（dummy action，纯粹是让物理仿真稳定
   下来，不是策略在决策）
3. 记录这一帧的观测（图像 + 状态）
4. 按任务编号轮流循环（task 0, 1, 2, ..., 9, 0, 1, 2, ...），凑够指定数量

也就是说，**校准数据本质上是"每个任务开局时的静态画面"，不包含任何策略真实
决策、真实交互产生的中间状态**。你原来 baseline 用这个脚本录 20 帧，我们这次
用同一个脚本录 50 帧，机制完全相同。

### 两边"选哪 10 个"的规则也一样

- **你原来的官方流程 baseline**：从 20 帧池子里，按"每个任务只留第一次出现
  的那一帧"过滤，10 个任务 → 10 帧。
- **我们这次的盲选**：直接取 50 帧池子的前 10 个（`pool[:10]`）。因为池子是
  按任务轮流生成的（0,1,2...9,0,1,2...9,...），前 10 个天然就是"每个任务各
  一帧"——和上面那条规则效果完全一样，只是从更大的池子（50 帧）里截出来的。

**唯一的真实差别**：池子深度（20 帧 vs 50 帧，截断/过滤前）、调用的包装脚本
不同。**不是**"在线 rollout vs 离线数据集"这种本质区别——两边都不是。

这也解释了一个之前没想通的现象：我们的盲选在 Spatial 上（98.4%）和你原来的
官方 baseline（98.0%）几乎打平——因为它们本来就是同一套采样逻辑，理论上应该
高度接近，这是个很好的"自洽性检验"，说明新流程复刻旧流程没有引入意外偏差。

**这也意味着一个需要注意的局限**：熵引导校准目前评估的"哪个观测更关键"，
问的是"策略第一眼看到这个任务初始画面时，多次重复采样得到的动作输出有多
一致/自信"，而不是"策略在任务执行过程中哪个中间状态更关键"——因为池子里
本来就只有各任务的开局画面，没有中间轨迹。这是一个可以在后续工作里改进的
方向：把观测池换成真实 rollout 轨迹，而不是任务开局静态画面。

## 三、熵引导采样具体怎么算

对 50 帧观测池里的每一帧，机制是：

1. **多次带噪声查询策略**：给同一个观测，用不同的随机种子调用
   `policy.infer()` 若干次（比如 6 次），因为 pi0.5 的动作头是基于流匹配/
   扩散的生成式模型，同一个输入配上不同的随机噪声起点，输出的动作序列会不
   完全一样——这个"不一样的程度"本身就是有意义的信号。
2. **算预测熵**：把这几次采样的动作输出，用核密度估计（KDE）拟合出一个概率
   分布，再算这个分布的熵。
   - **熵低** = 策略无论换哪个随机噪声，给出的动作都高度一致 → 策略"很确信"
     该怎么做 → 标记为"精度关键"（label=0）。这类情况下，如果量化引入的数值
     误差干扰了权重，很容易把这个本来很明确的决策带偏。
   - **熵高** = 策略换个随机种子输出差异很大，本身就很不确定/多解 → 标记为
     "冗余"（label=1）。这类情况下策略自己都拿不准，量化带来的小扰动相对
     不那么致命。
3. **HDBSCAN 聚类**：不是设一个固定的熵阈值切开两类，而是用基于密度的聚类
   算法，根据这批熵值本身的分布形状自适应地分成两簇——避免"到底熵多少算高"
   这种人为拍脑袋的阈值问题。
4. **校准样本选择**：从 10 个校准名额里，优先向 label=0（精度关键）倾斜，
   而不是像盲选那样不看信号硬选前 10 个。

整个信号链路是从 DemoSpeedup 这个另一篇论文的复现代码里迁移过来的（原本是
用来判断演示数据里哪些片段可以压缩、哪些必须保留），这里换了个用途：
判断校准数据里哪些观测更值得被精确保留。

## 四、三个 bug 的机理

### bug 1：校准时激活值采集全是空的

**设计初衷**：GR00T 自己的扩散/流匹配动作头，去噪要走好几步（比如 10 步），
每一步看到的激活值分布都不太一样。所以设计了一个"当前是第几步"的上下文变量
（Python 的 `ContextVar`，线程/异步安全），让校准钩子和量化层的前向函数知道
"现在该往第几步的统计量里累加/该用第几步的 scale"。

**bug 本身**：这个上下文变量，只有 GR00T 自己那套模型代码的去噪循环会去设置
它。但我们量化的是 pi0.5，pi0.5 的去噪循环是 openpi 项目自己实现的另一套代码，
完全不知道这个上下文变量的存在。结果就是：给 pi0.5 做校准时，"当前第几步"永远
读到空值，校准钩子因为读不到有效的步数标记，一个激活值都没记录下来——但流程
不报错，只是悄悄产出一个内容全是空的、坏掉的量化包。

**怎么修的**：写了一个"外挂"补丁（monkey-patch），照抄 openpi 官方去噪循环的
逻辑，但在每一步去噪前后用 `with` 语句手动进出那个上下文变量——不改 openpi
也不改 Omega-QVLA-official-baseline 的源码，只是在外面套一层。

（中间试过一版"编译器友好"的写法：把 `with` 语句换成两个新增的进入/退出函数，
直接加进 Omega-QVLA-official-baseline 的源码里，为了绕开 `torch.compile` 编译
这段循环时在 `with` 语句上产生的编译中断。后来验证发现这个编译中断只影响*离线
校准*这条代码路径本身的编译速度，跟下面说的"真正部署推理完全不用这个补丁"结
论合在一起看，这个优化对实际效果没有增益，就按照"不改动原始仓库"的要求撤销
了，改回最初的 `with` 语句写法。）

**补充说明**：这个补丁只在**离线打包校准阶段**用得上，真正跑评测/部署推理的
服务端代码完全没用这个补丁——因为查证后发现，部署时用的打包 kernel 是每次
推理都**动态**重新计算激活值的量化参数（不依赖校准时存的"第几步用哪个 scale"
这张表），所以这个"第几步"上下文只在离线校准阶段影响 GPTQ 的敏感度矩阵计算，
不影响线上推理。

### bug 2：旋转矩阵不能先烘焙进权重

**设计初衷**：DuQuant 会给激活值/权重做一次类似"旋转"的线性变换（旋转矩阵
`R`，满足 `Wx = (WRᵀ)(Rx)`，数学上结果不变），目的是把原本集中在少数几个通道
上的离群值打散到一个 block 里的所有通道上——量化最怕的就是一个 block 里有个
特别大的数把整个 block 的量化刻度拉得很粗，旋转能缓解这个问题，让 INT4 的
16 个刻度用得更均匀。

**正确用法**：GPTQ 求解权重量化时，是在"已经旋转过的坐标系"下求解的；真正
用这个权重做矩阵乘法时，kernel 要**在推理时对输入的激活值做同样的旋转**，
让两边保持在同一个旋转坐标系下，运算结果才对。

**bug 本身**：写了一个转换工具，带了个 `--fold-duquant-rotations` 选项，
想法是"把旋转矩阵直接乘进已经量化好的 INT4 权重里，存成新权重，这样推理时
就不用对激活值做旋转了"。问题是：INT4 权重已经是离散的整数了，`Rᵀ乘以INT4权重`
算出来的结果几乎不可能还精确落在 INT4 能表示的 16 个刻度上，所以这一步等于是
**在已经量化过的数据上又做了一次量化**（业内叫"二次量化"），误差会不受控制地
叠加放大。

**验证方式**：直接拿你自己历史上跑出好结果的量化包（里面保留旋转矩阵、不做
烘焙）和烘焙版本的包，跑同一套评测代码对比——烘焙版本精度直接崩到 0-40%，
不烘焙版本和历史记录的精度对得上，一步锁定问题就出在"烘焙"这一步。

**怎么修的**：转换的时候不加这个 flag，旋转矩阵作为独立的小张量保留在打包
文件里，kernel 在推理时对激活值做旋转（`rotate_input_blocks_wmma`），整个
链路里量化只发生一次。

### bug 3：量化层的部分张量卡在 CPU 上没搬到 GPU

**背景知识**：PyTorch 的模型参数/缓冲区都挂在具体某个设备（CPU 或某张 GPU）
上，`register_buffer` 是往一个模块上挂一个"跟着模型走"的张量，调用模型的
`.to(device)` 会把模型下面所有挂载的参数/缓冲区**递归地**一起搬到目标设备。

**bug 本身**：推理服务在装载量化包时，会把原始的 `nn.Linear` 层换成自定义的
`GptqLinear` 层，换层的过程中会从打包文件里读出旋转矩阵等辅助张量并挂到新层
上——这些张量是从硬盘读出来的，默认就停在 CPU 上，而这一步发生在"整个模型已经
搬到 GPU 之后"。换完层之后代码没有再调用一次 `model.to(device)`，于是这些新
挂上去的张量就一直留在 CPU，而模型主体和输入数据都在 GPU 上。

**造成的后果**：每次前向计算，只要用到这些卡在 CPU 上的张量（比如做旋转），
代码里写的是"用的时候临时 `.to(device)` 转一下"——这个转换每次推理调用都要
对 252 层量化层各做一遍，而不是加载模型时做一次。每一次 CPU→GPU 的搬运都有
固定的同步开销（不只是搬数据本身的时间，还有等 GPU/CPU 对齐状态的开销），
252 层乘起来就是一笔不小的、完全没必要的浪费。**同时**，这种"有时候会冒出一个
在 CPU 上的张量"的写法，会让 `torch.compile` 没法把这段计算编译成一整块高效
的 GPU 计算图——每次遇到这种张量都要打断编译好的执行路径，退回到逐行解释执行，
这又是一层额外的开销。

**怎么发现的**：直接查了量化后模型里每一层的张量，发现 252 层里全部 252 层
的旋转矩阵张量都显示在 CPU 上，而权重张量本身在 GPU 上——两者理应待在同一
设备，一对比就看出来了。

**怎么修的**：换完层之后补一次 `model.to(device)`，一次性把这些新挂上去的
张量搬到 GPU，以后每次推理就不用再临时搬运了。这个写法在同一份代码里，
DuQuant 和 RTN 两个分支其实已经这么做了（旁边还留了注释解释原因），唯独
GPTQ 这条分支漏掉了。实测这一个改动 + 另一处 kernel 融合优化，能把单次
推理调用时间从 1.39 秒降到 0.775 秒（提速 1.8 倍）。
