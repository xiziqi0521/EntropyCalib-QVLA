# 熵引导校准采样 × W4A4 量化（EntropyCalib-QVLA）系统说明

> 本文档用于周汇报材料。技术细节的英文工程文档见同目录 `README.md`；
> 本文聚焦"这个系统做了什么、为什么、效果如何"。

## 一、背景与动机

W4A4（权重4bit、激活4bit）后训练量化能大幅压缩 VLA 模型的显存和推理开销，
但精度损失主要来自**校准（calibration）阶段**：用少量真实观测样本估计激活值
分布，再据此求解 GPTQ 权重量化 / RTN 量化。现有流程（包括我们自己原来的
Omega-QVLA baseline）在校准样本的选择上是**盲选**的——要么取观测池的前 N 个，
要么按固定间隔均匀采样，没有考虑"这个时刻的观测对模型输出有多关键"。

同时，`/private/xzq/DemoSpeedup`（另一篇论文的复现项目）里有一套现成的算法：
对策略做多次带噪声的随机采样，用预测熵（predictive entropy）刻画每个时间步
"模型有多确信"，再用 HDBSCAN 把轨迹切成"精度关键（低熵）/ 冗余（高熵）"两类。
这个信号原本是用来做**演示数据压缩**的，但它本质上回答的是"哪些时刻的模型行为
最值得被精确保留"——这正好是校准样本选择需要的信号。

**核心思路**：把 DemoSpeedup 的熵检测算法迁移过来，仅用于**替换校准样本的选
择策略**，量化数学本身（GPTQ 求解、RTN、DuQuant 旋转、INT4 打包、自研 CUDA
kernel）完全不改，改动只发生在"喂给校准流程的是哪些观测"这一步。

按用户要求，所有新代码在全新目录 `/private/xzq/EntropyCalib-QVLA` 中开发，
不修改 `Omega-QVLA`、`Omega-QVLA-official-baseline`、`DemoSpeedup`、`openpi`
任何一行原始代码（唯一例外见下文"内核问题"一节，是经明确授权的两处工程 bug 修复）。

## 二、系统架构

```
50 个真实 LIBERO 观测（每个任务集单独录制）
        │
        ▼
对每个观测，用带噪声的策略多次采样 → 预测熵曲线（迁移自 DemoSpeedup）
        │
        ▼
HDBSCAN 聚类 → 0/1 标签（0=精度关键/低熵，1=冗余/高熵）
        │
        ▼
熵引导采样：从 50 个观测里选出 10 个，优先选精度关键的样本
   （对照组"盲选"：直接取观测池的前 10 个）
        │
        ▼
用这 10 个样本做校准（"官方流程"：PaliGemma 126层 GPTQ + Expert 126层 RTN，
共 252 层，group-64，自研 CUDA kernel）—— 量化数学代码完全未改动
        │
        ▼
LIBERO 全量评测（4 个任务集 × 10 个任务 × 50 次试验 = 每个任务集 500 局）
```

## 三、过程中定位并修复的关键问题

做实验的过程中发现并修复了三个和"能不能拿到真实、可信结果"直接相关的 bug：

1. **校准阶段激活值采集为空**：Omega-QVLA 的校准钩子依赖一个"当前是第几步
   去噪"的上下文变量，这个变量只有 GR00T 自己的模型代码会设置，openpi 的
   pi0.5 去噪循环完全不知道它的存在——导致校准时"采集到 0 个激活值"，产出的
   量化包是坏的却不报错。修复方式是给 pi0.5 的去噪循环外挂一个 monkey-patch，
   在每一步去噪时手动进入这个上下文（新建文件，不改 openpi 源码）。

2. **旋转矩阵不能提前烘焙进权重**：把 DuQuant 的旋转矩阵在打包前直接乘进权
   重、再做 INT4 量化，会造成"二次量化"，实测精度从 80-92% 掉到 0-40%。正确
   做法是保留旋转矩阵，在**推理时对激活值**做旋转（kernel 里的
   `rotate_input_blocks_wmma`）。这个 bug 是通过和用户自己历史上跑出好结果的
   量化包做逐字段比对定位到的。

3. **量化层的部分缓冲区遗留在 CPU 上，导致真实推理速度远低于预期**：
   `openpi_inference_service.py` 里 GPTQ 分支包完权重层之后忘了把模型重新
   `.to(device)`，导致 252 层里每一层的旋转/量化辅助张量全部卡在 CPU 上，
   每次前向都要做一次 CPU→GPU 拷贝。这是"W4A4 kernel 感觉比预期慢"这个疑问
   的根因，实测该问题 + torch.compile 融合优化，可以把单次推理调用从
   **1.39s 降到 0.775s（提速 1.8 倍）**。（这一修复目前还未合入生产评测脚本，
   见下文"待办"。）

## 四、实验结果

评测配置：checkpoint 为独立训练的 pi0.5（`pi05_independent_pytorch_l20_seed42`，
30000 步），4 个 LIBERO 任务集，每个任务集 10 个任务 × 50 次试验 = 500 局。

对照的"官方流程 baseline"是用户自己此前跑出的、同样是 252 层全量化配方的
W4A4 结果（`/private/xzq/Omega-QVLA/results/`），是目前记录里精度最可靠的
W4A4 基准。

| 任务集 | FP16 参考 | 官方流程 baseline | 熵引导校准（10样本）| 盲选校准（10样本）|
|---|---|---|---|---|
| Spatial | 98.6% | 98.0% | 97.0% | 98.4% |
| Goal | 95.6% | 96.8% | 95.4% | 跑测中 |
| Object | 97.8% | 98.8% | 98.6% | 跑测中 |
| Long (libero_10) | 91.0% | **84.2%** | **88.8%** | 跑测中 |

**核心发现**：

- **Long 任务集上熵引导校准明显优于原 baseline**：88.8% vs 84.2%，+4.6 个
  百分点，把和 FP16 的差距从 6.8pp 收窄到 2.2pp（收窄约 68%）。Long 是四个
  任务集里量化误差最敏感、精度损失最大的一个，也正是熵引导设计上应该最有
  用武之地的场景——现象和设计动机吻合。
- **Spatial/Goal/Object 上熵引导和原 baseline 基本持平**（±0~1.4pp），这三个
  任务集本身精度已经接近天花板（97-99%），可提升空间有限。Spatial 上甚至
  是"盲选"以微弱优势反超熵引导（98.4% vs 97.0%），初步分析：Spatial 任务的
  核心变化维度是物体/目标的**空间位置**，不是策略的决策不确定性，熵信号在
  这类任务上未必能筛出真正关键的校准样本。
- 盲选在 Spatial 上（98.4%）甚至略高于原 baseline（98.0%），说明新搭建的
  校准数据采集流程本身是扎实可靠的，提升不完全依赖熵引导本身。

**结论（阶段性）**：熵引导校准在"量化最吃力的任务集"上收益明确、可复现（在
小规模 5-trial 预实验和本次 500 局全量评测上都得到验证），在已经接近精度
天花板的任务集上收益不明显甚至轻微为负。这是一个符合直觉、有实际价值的
正向结果，而不是全面碾压式的提升。

## 五、当前进度 / 待办

- ✅ Object、Goal、Long、Spatial 四个任务集的熵引导全量评测（500局/集）已完成
- ✅ Spatial 的盲选全量评测已完成
- 🔄 Object、Goal、Long 的盲选全量评测正在并行跑测（GPU 0/5/6），预计还需
  数小时到十余小时不等
- 📌 推理速度优化（CPU→GPU 设备同步修复 + kernel 融合算子，1.8x 提速）已经
  验证但**尚未合入生产评测脚本**，当前所有跑测都还在用未优化的慢速路径
- 📌 三个任务集盲选结果跑完后，将得到完整的"熵引导 vs 盲选 vs 原 baseline"
  四任务集全对照表

## 六、代码位置

- 新增代码全部位于 `/private/xzq/EntropyCalib-QVLA/`（`entropy_calib/` 核心
  逻辑，`scripts/` 各阶段脚本，`cache/` 各任务集的中间产物和评测结果）
- 依赖的原始项目均只读引用，未做任何修改：`Omega-QVLA`、
  `Omega-QVLA-official-baseline`、`DemoSpeedup`、`openpi`
- 唯一的例外（经明确授权）：`Omega-QVLA-official-baseline/gr00t/quantization/
  dit_step_context.py` 增加了两个新函数（原有函数保持不变），以及
  `Omega-QVLA/gr00t/quantization/{w4a4_ops.cpp,omega_w4a4_backend.py}` 新增
  一个融合算子，均已在编辑前备份原文件
